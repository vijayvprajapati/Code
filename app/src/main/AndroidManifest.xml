<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codder.logibox">

    <application
        style="@style/textcolor"
        android:allowBackup="true"
        android:icon="@mipmap/logiboxx"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".SplashScreenMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginMainActivity" />
        <activity android:name=".NavidationMainActivity" />
        <activity android:name=".RegisteruserMainActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".AddOrderMainActivity" />
        <activity android:name=".FeedbackMainActivity" />
        <activity android:name=".ClaimMainActivity" />
        <activity android:name=".Myprofile"></activity>
    </application>

</manifest>