<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_forgot_password"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="60dp"
    android:paddingTop="60dp"
    android:paddingRight="30dp"
    android:paddingLeft="30dp"
    android:weightSum="2"
    android:orientation="vertical"
    android:gravity="center"

    tools:context="com.example.codder.logibox.LoginMainActivity">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:weightSum="2"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingBottom="50dp">
        <EditText
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:id="@+id/etEmailForgotPassword"
            style="@style/textcolor"
            android:layout_weight="1"
            android:layout_marginBottom="10dp"
            android:background="@drawable/rectedit"
            android:hint="Enter Your Email ID" android:textColorHint="@android:color/secondary_text_dark"/>
        <Button
            android:layout_width="150dp"
            android:layout_height="0dp"
            android:id="@+id/btnSubmit"
            android:layout_weight="1"

            android:text="Submit"/>
    </LinearLayout>

</LinearLayout>
